{
  "name": "hawza",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "deploy:pages": "npm run build && wrangler pages deploy dist --project-name hawza --branch main"
  },
  "dependencies": {
    "astro": "^7.2.4",
    "filepond": "^4.32.12",
    "filepond-plugin-image-preview": "^4.6.12"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "wrangler": "^4.125.0"
  }
}
